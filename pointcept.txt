Isambard:
Could you run 'clifton auth' followed by 'clifton ssh-config write' and let me know if that resolves your connection problems

0.安装conda环境

1. 拉取支持 5090 的官方镜像  权限拦路虎！🐯
我们需要 25.01 或更新的版本（通常 1 月发布的镜像开始支持 5090）。
# 拉取 NVIDIA PyTorch 镜像 (内含 CUDA 12.8+, PyTorch 2.5+, 且针对 Blackwell 优化)
sudo docker pull nvcr.io/nvidia/pytorch:25.01-py3

2. 启动容器 (挂载你的代码目录)
假设你在 ~/blz 目录下：

# --gpus all: 启用显卡
# --ipc=host: 防止多卡训练时内存不足 (Pointcept 必须)
# -v: 将当前目录挂载到容器内的 /workspace
docker run --gpus all -it --rm --ipc=host -v $(pwd):/workspace nvcr.io/nvidia/pytorch:25.01-py3

# 启动容器并挂载当前目录
sudo docker run --gpus all -it --rm --ipc=host -v $(pwd):/workspace nvcr.io/nvidia/pytorch:25.01-py3
# 进入容器后，直接运行 Python 验证
python -c "import torch; print(f'CUDA: {torch.version.cuda}'); print(f'Device: {torch.cuda.get_device_name(0)}'); a = torch.randn(1000).cuda(); print('Success! 5090 is READY.')"

exit 退出 sudo docker exec -it bole_lab bash 进入

# 退出当前容器
exit

# 挂载文件到docker
# 删除旧的容器
sudo docker rm -f bole_lab_ptv3
sudo docker rm -f bole_lab_final

# ptv3
sudo docker run --gpus all -it \
  --name bole_lab_ptv3 \
  --shm-size=32g \
  -v /home/bolezhang/blz/Pointcept:/workspace/Pointcept \
  -v /data/datasets:/data/datasets \
  ptv3_gpu_image /bin/bash
# 我的
sudo docker run --gpus all -it \
  --name bole_lab_final \
  --shm-size=32g \
  -v /home/bolezhang/blz/SuperPointcept:/workspace/SuperPointcept \
  -v /data/datasets:/data/datasets \
  ptv3_gpu_image /bin/bash
# 如果被关机
sudo docker start -ai bole_lab_final bash
sudo docker start -ai bole_lab_ptv3
# 进入
sudo docker exec -it bole_lab_ptv3 bash
sudo docker exec -it bole_lab_final bash

5.如有必要 检查：

python -c 'import pointops; print("PointOps is ready")'

python -c "import torch; print(f'Torch: {torch.__version__}, CUDA: {torch.version.cuda}')"


训练：

export CUDA_LAUNCH_BLOCKING=1

sh scripts/train.sh -g 1 -d stpls3d -c semseg-pt-v3m1-0-base -n exp_stpls3d_5090
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n SensatUrban-PTV3-Base
新的训练脚本：

sh scripts/train.sh -g 1 -d stpls3d -c semseg-pt-v3m1-0-base-6cls-focal -n exp_stpls3d_5090_6cls_focal
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base-focal -n exp_sensaturban_5090_focal
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n sensaturban_5090

新新的：

sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n sensaturban_5090
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n exp_sensaturban_5090_plus
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n exp_sensaturban_5090_plus_superCPE_GeoSPD


sh scripts/test.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n exp_sensaturban_5090_plus_superCPE -w model_last
sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n exp_sensaturban_5090_plus_superCPE 

sh scripts/train.sh -g 1 -d sensaturban -c semseg-pt-v3m1-0-base -n exp_sensaturban_5090_plus_superCPE -r true

tumx笔记

apt update
apt install -y tmux


tmux new -s ptv3_train
先按 Ctrl + b，然后按 d (Detach)
tmux attach -t ptv3_train

功能,快捷键 (Prefix + ...)
水平分屏 (Split Horizontal),""""
垂直分屏 (Split Vertical),%
关闭当前 Pane,x
切换 Pane,方向键 ↑ ↓ ← →
创建新窗口 (New Window),c
切换窗口 (Next Window),n